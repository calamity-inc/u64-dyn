name: C

on:
  push:
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Build
        working-directory: c
        run: gcc test.c u64_dyn.c -o test
      - name: Run tests
        working-directory: c
        run: ./test
