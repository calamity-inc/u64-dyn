name: Python

on:
  push:
    branches:
      - senpai
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run tests
        working-directory: python
        run: python test.py
